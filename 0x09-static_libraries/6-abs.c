#include <stdio.h>

/**
 * _abs - prints the absolute value of integer
 * @n: integer to get the abosulte
 * Return: the absolute value of integer.
 */
int _abs(int n)
{
if (n < 0)
{
return (n * -1);
}
else
return (n);
}
